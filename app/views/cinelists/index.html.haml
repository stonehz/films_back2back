%h1 Listing cinelists

%table
  %tr
    %th Object response
    %th
    %th
    %th

  - @cinelists.each do |cinelist|
    %tr
      %td= cinelist.object_response
      %td= link_to 'Show', cinelist
      %td= link_to 'Edit', edit_cinelist_path(cinelist)
      %td= link_to 'Destroy', cinelist, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Cinelist', new_cinelist_path
